# Supabase public project URL and browser-safe anon key.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Absolute origin used for guest registration links, without a trailing slash.
# This is read server-side when a front-desk user creates a link.
SITE_URL=
