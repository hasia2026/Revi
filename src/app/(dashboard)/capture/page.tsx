import { Inbox } from "lucide-react";
import { ModulePlaceholderPage } from "@/components/shared/ModulePlaceholderPage";

export default function CapturePage() {
  return (
    <ModulePlaceholderPage
      title="Capture"
      subtitle="The business's memory"
      icon={Inbox}
      description="Capture will become the intake system that routes what comes in toward Knowledge, Training, Company Compass, Customers, and Growth. Foundation only — routing engine not built yet."
    />
  );
}
